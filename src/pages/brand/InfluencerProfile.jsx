import { useParams, Link } from 'react-router-dom'
import { INFLUENCERS } from '../../data/mockInfluencers'
export default function InfluencerProfile(){ const { id }=useParams(); const inf=INFLUENCERS.find(i=>i.id===id); if(!inf) return <div className="card p-6">Influencer not found.</div>; return (<div className="space-y-6"><div className="card p-6"><h2 className="text-xl font-semibold">{inf.name}</h2><div className="text-base text-gray-600">Niche: {inf.niche.join(', ')}</div><div className="mt-4 grid gap-4 sm:grid-cols-2"><div className="rounded-2xl border p-4"><h3 className="font-semibold">📇 Contact & Social</h3><p className="text-base mt-1">Email: {inf.contact}</p><p className="text-base">Social: {inf.social}</p><Link to="/app/messages" className="btn btn-primary mt-3">💬 Send Message</Link></div><div className="rounded-2xl border p-4"><h3 className="font-semibold">📊 Performance Metrics</h3><p className="text-base mt-1">Followers: {inf.followers.toLocaleString('en-IN')}</p><p className="text-base">Total Views: {inf.totalViews.toLocaleString('en-IN')}</p><p className="text-base">Engagement: {inf.engagement}%</p></div></div></div><div className="card p-6"><h3 className="font-semibold">⭐ Brand Reviews</h3><ul className="mt-3 list-disc pl-5">{inf.reviews.map((r,i)=><li key={i} className="text-base"><b>{r.brand}:</b> {r.text} ({'⭐'.repeat(r.rating)})</li>)}</ul></div></div>) }
