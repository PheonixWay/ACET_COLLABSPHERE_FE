import { CAMPAIGNS } from '../../data/mockCampaigns'
import { Link } from 'react-router-dom'
export default function MyCampaigns(){ return (<div className="space-y-6"><h2 className="text-xl font-semibold">📋 My Campaigns</h2><div className="grid gap-6 md:grid-cols-2 xl:grid-cols-3">{CAMPAIGNS.map(c=>(<div key={c.id} className="card p-6 min-h-[220px]"><div className="text-xl font-semibold">{c.title}</div><div className="text-base text-gray-600 dark:text-gray-300">Budget: ₹{c.budget.toLocaleString('en-IN')}</div><div className="mt-2 text-base">Niche: {c.niche.join(', ')}</div><div className="mt-5 flex gap-3"><Link to={`/app/campaign/${c.id}`} className="btn btn-ghost">👁️ View</Link><Link to={`/app/campaign/${c.id}/review`} className="btn btn-primary">📝 Review Applications</Link><button className="btn btn-ghost" onClick={()=>alert('Campaign closed (mock)')}>🚫 Close</button></div></div>))}</div></div>) }
